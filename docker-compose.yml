# Docker Compose skeleton for Lenny Growth Assistant
# Service definitions will be configured as components are implemented.

services:
  backend:
    # Service implementation will be configured during backend setup
    image: hello-world
    # build:
    #   context: ./backend
    # ports:
    #   - "8000:8000"
    # env_file:
    #   - .env

  frontend:
    # Service implementation will be configured during frontend setup
    image: hello-world
    # build:
    #   context: ./frontend
    # ports:
    #   - "3000:3000"
    # env_file:
    #   - .env
